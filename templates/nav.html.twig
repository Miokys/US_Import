<header>

        <nav class="navbar navbar-expand-lg border-bottom border-body">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_home') }}"><img class="navimg" src="{{ asset('assets/medias/img/logo.png') }}" alt="USImport"></a>
                <button class="navbar-toggler bg-dark" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
                <span class="fs-1 text-light"><i class="bi bi-list"></i></span>
                </button>
                
                <div class="collapse navbar-collapse text-center p-2" id="navbarScroll">
                    <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll fw-bolder">
                        <!-- Liens de la Navbar -->
                        <li class="nav-item">
                            <a class="nav-link fs-3" href="{{ path('app_category_index') }}">Categories</a>
                        </li>
                        <!--Menu Admin -->
                            {% if is_granted('ROLE_ADMIN') %}
                                <li class="nav-item dropdown">
                                <a class="nav-link fs-3 dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Admin
                                </a>
                                <ul class="dropdown-menu">
                                    <li><a class="dropdown-item" href="{{ path('app_admin_category_index') }}">Categories</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_admin_product_index') }}">Produits</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_admin_user_index') }}">Utilisateurs</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_admin_review_index') }}">Avis Utilisateurs</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item">Reservé aux administrateurs</a></li>
                                </ul>
                            {% endif %}
                    </ul>
                    <!-- fin des liens de la Navbar -->

                    <!-- Barre de Recherche -->
                    <form class="nav-form" role="search" action="{{ path('app_lookup') }}" method="GET">
                        <input class="nav-form-input" name="search" type="search" placeholder="Rechercher" aria-label="Search">
                        <button class="nav-form-btn" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </form> 
                    <!-- Fin de la Barre de Recherche -->

                    <!-- Boutons de panier et de Connexion -->
                    <div class="nav-btn">
                        <a class="btn-cart" href="{{ path("app_cart") }}">
                            <i class="bi-cart-fill me-1"></i>
                            Panier {{ app.session.get('panier') ? app.session.get('panier')|length : '0' }}
                        </a>
                    {% if is_granted('ROLE_USER') %}
                        <a href={{ path('app_profile_show') }} class="btn-log">
                            <i class="bi bi-person-circle"></i> 
                            Mon compte
                        </a>
                        <a href={{ path('app_logout') }} class="btn-log">
                            <i class="bi bi-box-arrow-left"></i> Se déconnecter</a>
                    {% else %}
                        <a href={{ path('app_login') }} class="btn-log"><i class="bi bi-box-arrow-in-right"></i> Se connecter</a>
                    {% endif %}
                    <!-- fin des boutons -->
                    </div>
                </div>
            </div>
        </nav>
    </header>